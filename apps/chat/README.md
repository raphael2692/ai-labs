# Chat

Minimal Streamlit chat UI against the Unsloth server. Good starting point/reference for new apps that
only need the LLM, not Whisper.

## Run

```
uv run --package chat streamlit run apps/chat/app.py
```
