[project]
name = "meeting-minutes"
version = "0.1.0"
description = "Streamlit app: transcribe a meeting recording via the Whisper server, then summarize it via the Unsloth server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ai-lab-common",
]

[tool.uv.sources]
ai-lab-common = { workspace = true }

[tool.uv]
package = false
