[project]
name = "transcribe"
version = "0.1.0"
description = "Streamlit app: transcribe a recording via the Whisper server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ai-lab-common",
]

[tool.uv.sources]
ai-lab-common = { workspace = true }

[tool.uv]
package = false
