[project]
name = "unsloth-server"
version = "0.1.0"
description = "Unsloth Studio: OpenAI-compatible LLM inference server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "unsloth",
]

[tool.uv]
package = false
